# ARCADE — Handoff Document
> Создан: 24 июня 2026. Перенос сессии в новый чат.

---

## Цель проекта

**ARCADE** — Telegram Mini App (@arc_tonbot). Пользователи фармят монету **ARC** через рекламу, задания, рефералы, и играют в **PvP-рулетку**, ставя ARC.

Хостинг: Railway (`https://arcade-production-40a8.up.railway.app`)  
БД: Supabase (project ID: `rfmougjzanxugqucbrah`)  
Канал: https://t.me/arcare_ton  
Бот: @arc_tonbot

---

## Текущее состояние (24.06.2026)

### База пользователей
| Метрика | Значение |
|---|---|
| Всего юзеров | 653 |
| Новых сегодня | 0 |
| За 7 дней | 42 |
| DAU | ~75-85 |
| Подключили кошелёк | 88 (13%) |
| Сделали депозит TON | 6 (1%) |
| PRO активных | 2 |
| ARC на руках | 183 225 |
| TON на руках | 3.02 |

### Retention по когортам
- Когорта 9 июня (134 чел): D1=29%, D7=**40%** — хорошая когорта, продукт умеет держать
- Когорта 10 июня (247 чел): D1=4%, D7=5% — **мусорный трафик** (боты/дропхантеры)
- Остальные: D1=0-7%, D7=0-17%

### Воронка (за всё время)
```
Зарегистрировались:      653 (100%)
Смотрели рекламу:        166 (25%)
Ставили в PvP:           120 (18%)
Подключили кошелёк:       88 (13%)
Сделали депозит TON:       6 (<1%)  ← ГЛАВНАЯ ПРОБЛЕМА
Привели рефералов:        26 (4%)
Пришли по рефералке:     199 (30%)
```

### PRO подписка
- Куплено за всё время: **7 раз, 5 юзеров, 1.4 TON выручки**
- Цена: `PRO_PRICE_TON = 0.2`, срок: `PRO_DAYS = 7`
- Реф-комиссия при покупке: `PRO_REF_COMMISSION = 0.10` (10% в TON инвайтеру — уже в коде)

### Настройки БД (таблица `settings`)
| Ключ | Значение |
|---|---|
| arc_usd | 0.0001 |
| ton_usd | 1.56 (авто-обновляется с CoinGecko каждые 10 мин) |
| pvp_commission | 0.10 |
| exchange_daily_limit_ton | 5 |

---

## Что было сделано в этой сессии

### 1. Исправлен критический баг — приложение не открывалось
**Файл:** `public/js/app.js`  
**Проблема:** `else if (tab === 'pvp')` стоял после `else {` (ветка refs). В JS после `else` нельзя писать `else if`. Весь app.js не загружался → кнопки не работали.  
**Решение:** `else {` → `else if (tab === 'refs') {`  
**Коммит:** `8dfa430`

### 2. PvP лидерборд — обновлены правила конкурса
**Файл:** `server/index.js` строка ~705  
- Дата окончания: 7 июля → **4 июля**
- Логика: вместо `transactions (type='pvp')` → **`game_bets JOIN games`** где `status='done' AND winner_tg_id IS NOT NULL`
- Одиночные раунды (с возвратом) **не считаются**

**Файл:** `public/js/i18n.js` — обновлены все 7 языков  
**Коммит:** `33e07a6`

### 3. Создан промокод через Supabase
- Код: **ARC400VIP** | 400 ARC | 1 активация | активен

---

## Архитектура — полная карта файлов

```
server/
  index.js       — все API маршруты (961 строка), точка входа
  auth.js        — HMAC-SHA256 валидация Telegram initData
  db.js          — Supabase клиент
  helpers.js     — isPro(), adMultiplier(), changeArc(), changeTon(), payReferrals()
  game.js        — PvP рулетка (in-memory состояние!)
  deposits.js    — polling tonapi.io каждые 8 сек
  inactivity.js  — штрафы за неактивность (каждый час)
  reminders.js   — напоминания вернуться (каждый час)

bot/
  bot.js         — Telegram бот, /admin команды

public/
  index.html     — единый HTML, версия скриптов сейчас v=108
  js/app.js      — весь UI (~1600 строк), 5 табов
  js/i18n.js     — переводы 7 языков: ru, en, hi, pt, id, bn, vi

db/
  schema.sql     — DDL (не всегда 100% совпадает с реальной БД!)
```

---

## Механики которые уже работают в коде

### Реклама (8 форматов)
| Тип | Сеть | Лимит обычный | Лимит PRO | ARC за 1 (PRO) |
|---|---|---|---|---|
| ad | Adsgram S2S | 10 | 15 | **19** |
| ad4 | OnClickA | 10 | 15 | 9 |
| ad5 | Monetag | 5 | 10 | 9 |
| ad6 | Nygma | 5 | 10 | 9 |
| ad7 | Telega.io | 10 | 15 | 9 |
| ad8 | TADS | 5 | 10 | 9 |
| ad_short | Adsgram interstitial | 3 | 5 | 9 |
| ad_task | Adsgram task | 5 | 5 | 9 |

PRO множитель = 1.5 × 1.25 = **×1.875**  
Cooldown между просмотрами одного типа: **15 секунд** (защита от скриптов)  
PRO активный: ~870 ARC/день только с рекламы (без ad_task)

### Обмен TON→ARC (уже работает!)
**Маршруты:** `POST /api/exchange/info` и `POST /api/exchange/ton-arc`  
Курс: `Math.round(ton_usd / arc_usd)` = ~15 600 ARC за 1 TON  
Дневной лимит: `exchange_daily_limit_ton = 5` TON  
Таблица: `exchange_daily`

### Обмен ARC→TON (НЕ существует — надо писать)
Это главная планируемая фича. Подробности ниже.

### Неактивность (`server/inactivity.js`)
Запускается каждый час:
- День 5: предупреждение в бот
- День 6: последнее предупреждение
- День 7: **−20% ARC** + уведомление
- День 8+: **−5% ARC каждый день** пока не зайдёт

### Напоминания (`server/reminders.js`)
- Для юзеров неактивных 24ч-5д (дальше подхватывает inactivity.js)
- Отправляет push в боте с кнопкой «Зайти»
- Учитывает часовой пояс по языку (RU=+3, HI=+5.5, PT=−3 и т.д.)
- Не чаще 1 раза в 20 часов на юзера

### Broadcast (`/api/admin/broadcast`)
Защищён статическим секретом в заголовке `x-secret` (смотри в коде `server/index.js`).  
Поддерживает тексты на всех 7 языках, кнопка «Open ARCADE»

---

## Главная планируемая фича: обмен ARC→TON

### Концепция
- Три тарифа: PRO (есть), **PLUS** (новый), **PRO PLUS** (новый)
- Каждый тариф открывает доступ к обмену ARC→TON
- Лимит обмена в день = только ARC заработанный сегодня из рекламы
- Задания/чек-ин/рефералы — копятся отдельно, для будущего общего обмена

### Параметры тарифов

| | PRO | PLUS | PRO PLUS |
|---|---|---|---|
| Цена | 0.2 TON | 0.5 TON | 2.0 TON |
| Срок | 7 дней | 7 дней | 14 дней |
| Реклама/день | 85 | 85 | 85 |
| % фарма для обмена | 60% | 80% | 100% |
| Макс. обмен за период | ~0.25 TON | ~0.60 TON | ~2.30 TON |
| Юзер активный | +0.14 TON | +0.10 TON | +0.30 TON |
| Юзер ленивый | −0.05 | −0.15 | −0.40 |
| Тебе с активного | ~1.05 TON | ~0.97 TON | ~1.74 TON |

(при CPM $3, TON $1.60)

### Курс обмена ARC→TON
```
Рынок:      1 ARC = arc_usd / ton_usd = 0.0001 / 1.56 = 0.0000641 TON
Обмен -13%: 1 ARC = 0.0000558 TON  →  17 900 ARC = 1 TON
```
13% дисконт = чистый доход платформы с каждого обмена.

### Защита от потери денег
Твоя ключевая механика: **выплата всегда ≤ ARC заработанному рекламой сегодня.**  
Если CPM просядет → юзер просто меньше заработает → платформа не теряет.  
Ленивый юзер → вышел в минус → ты в плюсе.

### Порядок запуска
1. Получить первый вывод с рекламы ($50 минимум у Adsgram)
2. Узнать реальный CPM точно
3. Запустить обмен только для PRO (тест)
4. Добавить PLUS и PRO PLUS
5. Открыть общий обмен для всех (сначала — защита от мультиаккаунтов!)

---

## Реальные проблемы (по приоритету)

1. **Ценность ARC = 0** — нет причины фармить. Решение: обмен ARC→TON выше.
2. **Трафик остановился** — 0 новых сегодня. Причина: нет бюджета на рекламу.
3. **Воронка течёт** — 88 кошельков, 6 депозитов. 93% не платят.
4. **PvP держат 2 человека** — Karoboev и Gal15_K. Без них рулетка пустая.

---

## Достижения

- ✅ PvP рулетка живая — 38+ реальных раундов в день после запуска конкурса
- ✅ Конкурс (24 июня — 4 июля) сразу поднял объём PvP
- ✅ Retention D7=40% у качественной когорты — продукт умеет держать
- ✅ Неактивность сжигает ARC (−20% на 7й день, −5%/день дальше)
- ✅ TON→ARC обмен уже работает
- ✅ 8 рекламных сетей интегрировано
- ✅ 7 языков поддерживается

---

## Важные детали кода

### Нельзя делать
- Никогда не делай UPDATE `balance_arc` / `balance_ton` напрямую. Только через `changeArc()` и `changeTon()` из `server/helpers.js`
- ES-модули везде — только `import/export`, никакого `require()`
- PvP state — только в памяти. При рестарте Railway текущий раунд теряется

### Версия скриптов
После любой правки JS файлов в public/ — бампай версию в `public/index.html`:
```html
<script src="/js/app.js?v=108"></script>  ← меняй число
```

### Проверка синтаксиса перед пушем
```bash
node --check server/index.js
node --check public/js/app.js
node --check public/js/i18n.js
```

### Git
```bash
git add <файлы>
git commit -m "описание"
git push origin main
```
Railway деплоит автоматически.

### Возможный баг в schema.sql
В `schema.sql` столбец называется `ton_used`, но в коде (`index.js`) используется `used_ton`.  
Реальная БД создавалась не по schema.sql — проверь через Supabase если обмен будет глючить.

### Столбцы users которых нет в schema.sql
- `last_active_at` — обновляется при каждом `/api/me`
- `last_penalty_at` — дата последнего штрафа неактивности
- `last_reminder_at` — дата последнего напоминания
- `bot_blocked` — бот заблокирован юзером

### Таблицы в schema.sql которые не используются
- `lottery_rounds` — лотерея (заготовка, маршрутов нет)
- `lottery_tickets` — то же

---

## Следующие шаги (в порядке приоритета)

- [ ] **1.** Дождаться первого вывода с рекламы → узнать точный CPM
- [ ] **2.** Реализовать `POST /api/exchange/arc-ton` — обмен ARC→TON для PRO
- [ ] **3.** Добавить тарифы PLUS (0.5 TON) и PRO PLUS (2 TON) в код
- [ ] **4.** Обновить UI — добавить экран обмена в профиль
- [ ] **5.** Перед общим обменом — защита от мультиаккаунтов
- [ ] **6.** Открыть общий обмен для всех — разблокирует накопленный ARC

---

## Как работать в новом чате

1. **Скинь этот файл первым сообщением** — достаточно, Клод поймёт контекст
2. Говори конкретно: не «улучши проект», а «реализуй маршрут /api/exchange/arc-ton»
3. Суpabase MCP подключён — можно запрашивать данные из БД напрямую
4. Давай разрешение на git push сразу в начале чата чтобы не останавливаться
5. Клод иногда соглашается чтобы избежать конфликта — требуй честного ответа, он может
