# MINER-DESIGN.md — Чертёж системы «Майнер» (Реактор)

> Финальная концепция, согласована 02.07.2026.
> НЕ СТРОИТЬ, пока не пройден гейт: **первый реальный вывод денег с рекламных сетей.**
> После вывода: подставить реальный CPM в `settings.ad_cpm_usd` и строить по этому файлу.

---

## 1. Суть — смена мышления игрока

| Было | Станет |
|---|---|
| «Фармлю ARC, жду обмена, надеюсь» | «У меня работает машина за мои деньги. Не зайду — она стоит, я теряю» |

Майнер = устройство, купленное за TON (или ARC), которое приносит TON,
**только пока заряжено**. Заряжается — просмотром рекламы. Реклама из
«задания за монетку» превращается в **топливо для собственного актива**.

---

## 2. Три майнера (тарифы)

| | 🟢 MINI | 🔵 CORE | 🟣 MAX |
|---|---|---|---|
| Цена | 0.5 TON | 2 TON | 5 TON |
| Срок | 14 дней | 14 дней | 21 день |
| Заряд с 1 рекламы | +4% | +6% | +8% |
| Реклам/день (цель) | 40 | 55 | 70 |
| Реклам за срок | 560 | 770 | 1470 |
| Доля юзера от рекламного дохода | 14% | 42% | 62% |
| Подходов зарядки в день | 3 | 4 | 4-5 |
| Майнер работает в сутки | ~5 ч | ~10 ч | ~16-17 ч |

**Экономика при CPM $2, TON $1.54** (1 реклама = 0.0013 TON платформе):

| | MINI | CORE | MAX |
|---|---|---|---|
| Доход платформы с рекламы юзера | 0.73 | 1.00 | 1.91 |
| Юзер получает сверху депозита | +0.10 | +0.42 | +1.19 |
| ROI юзера | +20% | +21% | +24% |
| Платформе чистыми (худший случай, реф 5%) | **+0.60** | **+0.48** | **+0.48** |

Депозит возвращается юзеру целиком в конце срока + добытое.
Плюс депозиты лежат на платформе весь срок (оборотка).

---

## 3. Формула дохода (главная защита от минуса)

```
ad_value_ton  = ad_cpm_usd / 1000 / ton_usd          — стоимость 1 просмотра платформе
доход юзера   = просмотрено_реклам × ad_value_ton × доля_майнера × мощность
```

**Железные правила:**
1. Юзеру платится % от ФАКТИЧЕСКИ открученной рекламы. Не фикс.
   CPM упал → доход юзера упал сам. Платформа не доплачивает НИКОГДА.
2. Заряд 0% → доход 0. Ленивый юзер наказывает себя сам, его депозит у платформы.
3. Выплата (депозит + добытое) — в конце срока. Бросил в середине → хвост не получил.
4. Реф-комиссия = 5% от цены покупки (которую платформа уже получила), не от дохода.

**Минус невозможен ни при каком CPM.**

---

## 4. Заряд и мощность

Заряд тает линейно: **100% → 0% за 3 часа** (~0.55%/мин). Фронт показывает каждый %.

| Заряд | Мощность (множитель дохода) |
|---|---|
| 100–80% | 100% |
| 79–60% | 90% |
| 59–40% | 75% |
| 39–20% | 50% |
| 19–1% | 20% |
| 0% | ⛔ остановка |

Ночь (8ч сна): лёг со 100% → работает ещё ~3 ч → стоит до утра.
Утренняя зарядка = обязательный ежедневный ритуал. Это фича, не баг.

**Реализация без кронов:** добыча считается лениво. В `user_miners` хранится
`charge` + `charge_updated_at`. При любом запросе (state/зарядка):
интегрируем прошедшее время по диапазонам мощности (линейный спад — считается
аналитически), прибавляем к `mined_ton`, обновляем `charge` и timestamp.

---

## 5. Покупка за ARC (путь бесплатных)

- Цена в ARC = цена_TON × текущий курс (`ton_usd / arc_usd`), сейчас ~15 400 ARC за 1 TON.
  MINI ≈ 7 700 ARC (+ можно наценку, решить при запуске).
- Депозита нет → возврата депозита нет.
- Выплата = **70% от добычи TON-покупателя** (−30%).
  Пример: TON-покупатель MINI вернул всего 0.60 → ARC-покупатель получит 0.42 TON.
- Смысл для юзера: превратил бесплатно нафармленный ARC в живые TON. Это скрытый
  обмен ARC→TON через работу. Кнопка «ARC→TON скоро» в профиле может вести в магазин.
- Платформе: его реклама 0.73 − выплата 0.42 = +0.31 TON, плюс сожжено 7 700 ARC из оборота.

Путь новичка без денег: реклама → ARC (с чек-ин множителем) → за 2-3 недели MINI за ARC → TON → вывод.

---

## 6. Что происходит с существующими механиками

| Механика | Решение |
|---|---|
| **PRO** | **УДАЛЯЕТСЯ.** Кнопку покупки убрать. Двум активным (`pro_until`) дать доходить до конца. Монетизация теперь = майнеры |
| Лимиты рекламы | Открыть PRO-лимиты ВСЕМ: итого **83/день** (было 53/85) |
| PvP-плюшки PRO (комиссия 5%, ставка 2000) | Передать **владельцам активного майнера** |
| Авто-чек-ин PRO | Умирает вместе с PRO |
| **Чек-ин** | Только для тех, у кого НЕТ майнера (множитель ARC ×1.0–1.5). На майнер не влияет никак |
| **Реклама (8 сетей)** | Код почти не трогаем. Есть активный майнер → награда = заряд (+4/6/8%) **+3 ARC фикс** (без множителей). Нет майнера → ARC как сейчас |
| **PvP** | Без изменений. +3 ARC владельцам майнеров держит рулетку живой |
| **Задания** | Как есть + новые: «держи заряд >80% весь день», «заряди N раз» |
| **Рефералка** | Старые 20%/10% ARC остаются. НОВОЕ: **5% в TON с покупки майнера рефералом** (механика как PRO-комиссия сейчас, `changeTon(..., 'referral', ...)`) |
| **Неактивность** | Работает как есть (−20%/−5% ARC). Майнер наказывает сам: разрядился = стоит |
| **Обмен TON→ARC** | Остаётся |
| **Обмен ARC→TON** | НЕ строим отдельно. Майнер за ARC и есть этот обмен |

---

## 7. UI — экраны

### Навигация (нижние табы)
```
PvP | Задания | 🏠 Главная | 🛒 Магазин (вместо Друзья) | Профиль
```
Рефералка → кнопка «Реферальная система» в Профиле, открывает прежний экран Друзей.

### Главная — 2 автоматических режима (сервер отдаёт has_miner)

**Без майнера** — как сейчас (баланс, лента Реклама 1-8 → +ARC, чек-ин)
+ баннер «⚡ Купи майнер — зарабатывай TON» → Магазин.

**С майнером:**
```
⚡ CORE · день 5/14
▓▓▓▓▓▓▓░░░ 68% · встанет через 2ч 02м
Добыто: 0.31 TON · мощность 90%
────────────────────────────────
[та же лента Реклама 1-8]  ← но награда: «+6% ⚡ +3 ARC»
```
Чек-ин блок на главной скрыт для владельцев майнера.

### Магазин
3 карточки (MINI/CORE/MAX): цена, срок, реклам/день, «вернёшь ~X TON».
Две кнопки: **[Купить за TON]** / **[Купить за ARC (−30% дохода)]**.
Калькулятор внизу пересчитывается от `ad_cpm_usd` живьём.

### Профиль
+ блок «Мои майнеры» (активный: прогресс; история: заработано)
+ кнопка «Реферальная система».

---

## 8. База данных

```sql
-- Типы майнеров (конфиг, редактируется без деплоя)
create table if not exists miner_types (
    code text primary key,              -- 'mini' | 'core' | 'max'
    title text not null,
    price_ton numeric(20,9) not null,
    days int not null,
    charge_per_ad numeric(6,3) not null,   -- 4 / 6 / 8 (%)
    ads_per_day int not null,              -- 40 / 55 / 70 (инфо для UI)
    user_share numeric(6,4) not null,      -- 0.14 / 0.42 / 0.62
    is_active boolean default true
);

insert into miner_types values
  ('mini', 'MINI', 0.5, 14, 4, 40, 0.14, true),
  ('core', 'CORE', 2.0, 14, 6, 55, 0.42, true),
  ('max',  'MAX',  5.0, 21, 8, 70, 0.62, true)
on conflict (code) do nothing;

-- Купленные майнеры
create table if not exists user_miners (
    id bigserial primary key,
    tg_id bigint not null,
    type_code text references miner_types(code),
    paid_currency text not null,           -- 'TON' | 'ARC'
    paid_amount numeric(20,9) not null,
    charge numeric(6,3) default 100,       -- 0–100
    charge_updated_at timestamptz default now(),
    mined_ton numeric(20,9) default 0,     -- добыто (лениво пересчитывается)
    started_at timestamptz default now(),
    ends_at timestamptz not null,
    status text default 'active',          -- active | finished | claimed
    last_low_notify_at timestamptz,        -- антиспам пушей
    created_at timestamptz default now()
);
create index if not exists idx_user_miners_active on user_miners(tg_id) where status = 'active';
```

**Ограничение: 1 активный майнер на юзера** (проверка в /api/miner/buy).

В `settings` добавить:
| Ключ | Значение | Зачем |
|---|---|---|
| `ad_cpm_usd` | '2.0' | **Единственная ручка.** Менять командой бота `/cpm 2.4` |

(`ton_usd` уже автообновляется с CoinGecko каждые 10 мин; `arc_usd` есть.)

В `users` уже есть заготовки `tier`, `tier_until`, `tier_bonus_date` — можно
не использовать, вся логика в `user_miners`.

---

## 9. API маршруты (новые)

```
POST /api/miner/shop    — типы + живой расчёт «вернёшь ~X TON» от текущего CPM
POST /api/miner/buy     — {type_code, currency: 'TON'|'ARC'}
                          атомарно через RPC (по образцу try_buy_pro):
                          списать цену, создать user_miner, реф 5% TON инвайтеру
POST /api/miner/state   — заряд/мощность/добыто/осталось дней (лениво пересчитать)
POST /api/miner/claim   — по окончании срока: depозит (если TON) + добыча
                          (×0.7 если куплен за ARC) → balance_ton
```

**Зарядка — НЕ отдельный маршрут.** Существующие обработчики рекламы
(`/api/adsgram/reward1`, `/api/ads/watch-short|4|5|6|7|8`, task-reward):
если у юзера активный майнер → `charge = min(100, charge + charge_per_ad)`
+ `changeArc(tgId, 3, тип, ...)`, иначе — ARC как сейчас.
Лимиты по сетям не меняются (кроме открытия PRO-лимитов всем).

**Изменения /api/me:** добавить `has_miner`, `miner: {type, charge, mined_ton, ends_at, power}`.

---

## 10. Уведомления бота (через существующий reminders-механизм)

- «⚡ Заряд 15% — майнер скоро встанет»  (не чаще 1 раза в 3 часа, `last_low_notify_at`)
- «⛔ Майнер остановился — доход не капает»
- Утром: «Майнер простаивает с 03:40, заряди» (учёт часового пояса по языку — уже есть)
- «✅ Срок окончен! Забери X TON» (кнопка → приложение)

Максимум 2-3 пуша/день на юзера — иначе блокируют бота.

**Новая команда бота (ADMIN_ID):** `/cpm 2.4` → пишет `settings.ad_cpm_usd`.
Также `/miners` — сводка: активных майнеров, добыто всего, ожидаемые выплаты.

---

## 11. Анти-абуз (обязательно ДО запуска ARC-майнеров)

1. 1 активный майнер на аккаунт.
2. Майнер за ARC: аккаунт старше 7 дней + ≥50 реклам в истории (реальный юзер).
3. Кулдаун 15 сек между рекламами одного типа — уже есть.
4. Вывод TON — вручную через админа (уже так работает, `notifyAdminWithdraw`).
5. Дневной лимит покупок майнеров за ARC глобально (первое время, напр. 10/день) —
   мониторить руками.

---

## 12. Юридическая осторожность

Формулировки в UI: «аренда майнера», «буст фарма», «добыча».
НЕ писать: «инвестиция», «доходность», «гарантированный доход», «ROI».
Telegram периодически чистит инвест-механики — обёртка должна быть игровой.

---

## 13. Порядок запуска

```
ГЕЙТ ⛔  Первый реальный вывод с рекламной сети ($50 у Adsgram)
   ↓     Узнать реальный CPM → выставить /cpm
1. Миграция БД (miner_types, user_miners, settings.ad_cpm_usd)
2. Бэкенд: маршруты + врезка зарядки в рекламные обработчики + RPC buy
3. Убрать PRO (кнопку), открыть лимиты 83 всем, PvP-перки владельцам майнеров
4. Фронт: главная (2 режима), Магазин вместо Друзей, рефералка в Профиль
5. Пуши бота + /cpm + /miners
6. Тест на себе (ADMIN_ID) с укороченным сроком (1 день)
7. Запуск ТОЛЬКО TON-майнеров (депозитных) — 1-2 недели обкатки
8. Первые выплаты → скриншоты в канал (главный маркетинг!)
9. Включить покупку за ARC (анти-абуз из п.11 обязателен)
```

---

## 14. Чего НЕ делать

- Не обещать фиксированные суммы дохода — только «до X при полной зарядке».
- Не платить из депозитов новых юзеров старым — только из рекламного дохода. Депозиты неприкосновенны до возврата.
- Не открывать ARC-майнеры одновременно с TON-майнерами — сначала обкатка на депозитных.
- Не удалять чек-ин/PvP/задания — они получили роли в системе.
- Не менять `balance_arc`/`balance_ton` напрямую — только `changeArc`/`changeTon` (как всегда).
